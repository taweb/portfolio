import React from 'react';
import styled from 'styled-components';

const SkillsContent = styled.div`
    text-align: center;    
`

const Skills = () => {
    return (
        <>
            <h2>Skills</h2>
            <SkillsContent>
                <p><strong>Angular / React / Javascript / Typescript / Vue.js / HTML / CSS / Git / Gatsby / NextJS / OpenAI</strong></p>
            </SkillsContent>
        </>
    );
}
 
export default Skills
